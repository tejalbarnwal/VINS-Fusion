## Architecture for VINS

#### Filesystem
* There are 4 pkgs: `camera_models`, `global_fusion`, `local_fusion` and `vins`
* Other directories like `config` and `docker` contain configuration and docker setup files respectively.
* 